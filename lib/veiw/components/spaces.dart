import 'package:flutter/material.dart';
import 'package:get/get.dart';

SizedBox buildHeightSpace(double value) => SizedBox(
      height: Get.width * value,
    );
SizedBox buildWirthSpace(double value) => SizedBox(
      width: Get.width * value,
    );
